import React from "react"
import Days from "../days.json"

const Calendar = () => (
    <div className="calendar">
        {console.log(Days)}
        {/* {Days.map((day, message) => {
            {console.log(day)}
        })} */}
    </div>

)

export default Calendar